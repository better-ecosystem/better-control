tab_source_files = files()


if get_option('network-tab')
    # tab_source_files += files('network.c')
endif

if get_option('volume-tab')
    tab_source_files += files('volume_logic.c')
    tab_source_files += files('volume.c')
endif